import StyledFirebaseAuth from "react-firebaseui/StyledFirebaseAuth";
import styled from "styled-components";

export const StyledFirebaseAuthStyled = styled(StyledFirebaseAuth)`
  height: 100vh;
  display: flex;
  align-items: center;
`;
